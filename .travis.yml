language: php
php:
  - 5.5
  - 5.4
  - 5.3

before_script:
  - composer self-update && composer --version
  - composer install

script: ./vendor/bin/phpunit --coverage-text --configuration tests/phpunit.xml tests/

notifications:
  email:
    - dev@funivan.com